spring:
  datasource:
    url: jdbc:h2:mem:jackpot-test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;LOCK_TIMEOUT=10000
  kafka:
    consumer:
      group-id: jackpot-service-integration-test

logging:
  level:
    org.apache.kafka: ERROR
    org.apache.zookeeper: ERROR
    kafka: ERROR
    state.change.logger: ERROR
